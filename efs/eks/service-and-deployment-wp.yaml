apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  ports:
    - port: 80
  selector:
    app: wordpress
    tier: frontend
  type: LoadBalancer
---
apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  selector:
    matchLabels:
      app: wordpress
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: wordpress
        tier: frontend
    spec:
      containers:
      - image: wordpress:6-apache
        name: wordpress
        env:
        - name: WORDPRESS_DB_HOST
          value: wordpress-mysql
        # WORDPRESS_DB_USER is the one that is required: the 4.8 image defaulted it to
        # root, while the current image defaults it to the literal string
        # 'example username', so leaving it unset fails with "Error establishing a
        # database connection". WORDPRESS_DB_NAME is set explicitly to pair with
        # MYSQL_DATABASE below; it happens to equal the image default, and omitting it
        # works today, but stating it means a future default change cannot surprise us.
        - name: WORDPRESS_DB_NAME
          value: wordpress
        - name: WORDPRESS_DB_USER
          value: wordpress
        - name: WORDPRESS_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-password
              key: wordpress-password
        ports:
        - containerPort: 80
          name: wordpress
        volumeMounts:
        - name: wordpress-persistent-storage
          mountPath: /var/www/html
      volumes:
      - name: wordpress-persistent-storage
        persistentVolumeClaim:
          # efs-pv-claim, not efs-pvc-claim. The only PVC defined for this variant is
          # efs/eks/pvc.yaml -> efs-pv-claim, so the old name resolved to nothing and this
          # pod could never be scheduled. Note both deployments now name the same claim:
          # that is how the single-PVC EFS variant was written, and it means MySQL's datadir
          # and WordPress's html tree land on one file system root. Separate subPath mounts
          # would be the proper fix; that needs an EFS file system to test against and is
          # deliberately not attempted here.
          claimName: efs-pv-claim
