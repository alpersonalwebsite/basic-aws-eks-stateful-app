# macOS. Three of these were committed (ebs/, ebs/eks/, efs/), which is what happens
# without this file.
.DS_Store

# Logs
*.log

# Environment and anything that looks like a credential
.env
.env.*
*.pem
*.key
credentials*
secrets/

# Local kubeconfigs, which carry cluster credentials
kubeconfig
kubeconfig.yaml
